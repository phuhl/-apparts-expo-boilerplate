import { StyleSheet } from "react-native";
// import { error, success, background } from "./colors";

const globalStyles = StyleSheet.create({
  shadow: {
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 4
    },
    shadowOpacity: 0.1,
    shadowRadius: 4.65,
    elevation: 16
  }
});

export default globalStyles;
